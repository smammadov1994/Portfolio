## Client-exposed env vars (Vite)
# AI chat API key (do NOT commit your real key)
VITE_Z_AI_API_KEY=your_api_key_here

# Cloudflare public endpoint for gallery listing.
# Recommended: point this at your Worker endpoint that returns JSON like { "images": ["..."] }.
# Example:
# CLOUDFLARE_PUBLIC_API=https://your-worker.your-subdomain.workers.dev/images
CLOUDFLARE_PUBLIC_API=

# Resend API key (server-side only). Used by the dev server email endpoint (/api/send-email).
# In production, do NOT expose this in the browser; use a Worker/Server.
RESEND_API_KEY=

# Optional: if you deploy an email-sending backend, point the client at it.
# If unset, the app uses /api/send-email (dev middleware).
VITE_CONTACT_API_URL=


